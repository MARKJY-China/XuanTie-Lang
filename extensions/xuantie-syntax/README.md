# XuanTie Syntax Highlighting

玄铁 (XuanTie) 编程语言的官方 VS Code 扩展包。
当前支持版本: v0.16.2

## 功能特性

- **语法高亮**：支持玄铁语言的所有关键字、运算符及内置函数。
- **代码片段**：内置拼音缩写快捷输入 (如 `bl` -> `变量`, `hs` -> `函数`)。
- **图标支持**：为 `.xt` 文件提供专属的高清图标。

## 使用方法

安装此扩展后，所有以 `.xt` 结尾的文件将自动开启语法高亮。
