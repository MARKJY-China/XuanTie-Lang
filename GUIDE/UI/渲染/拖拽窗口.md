# 拖拽窗口

**可以自定义某个控件在被触发的时候进入拖拽窗口状态。**

```xt
"触发": "按下", "点击": 函() { R.拖拽窗口() }
```

### 示例
```xt
设 状态 = UI.造状态(空)

UI.应用(状态, 函(s) {
    返 UI.叠([
        // 自绘标题栏(双锚横拉,高 40;按下空白处即 Windows 原生拖拽移动)
        UI.叠([
            UI.行([
                UI.文本("  铸造厂(无边框演示)", {"字号": 14, "字色": UI.色彩["浅灰"]}),
                UI.弹性(1, {}),
                UI.图标按钮("./assets/_ic_min.svg", {"点击": 函() { R.最小化窗口() }}),
                UI.图标按钮("./assets/_ic_max.svg", {"点击": 函() {
                    若 R.窗口最大化了?() { R.还原窗口() } 否 { R.最大化窗口() }
                }}),
                UI.图标按钮("./assets/_ic_close.svg", {
                    "悬底色": UI.色(220, 60, 70, 255), "点击": 函() { R.请求关闭窗口() }
                })
            ], {"叉轴对齐": UI.叉中, "内边距": UI.四边(8, 4, 8, 4), "间距": 4,
                "触发": "按下", "点击": 函() { R.拖拽窗口() }})
        ], {"锚": {"左": 0, "上": 0, "右": 1}, "高": 40, "底色": UI.色(36, 40, 52, 255)})
        // 主区:图标按钮陈列 + 计数
    ], {})
}, {"宽": 640, "高": 360, "标题": "无边框演示", "无边框": 真, "可调整": 真})
```

<video controls style="max-width:100%;border-radius:10px"><source src="https://xuantie-community.cn-nb1.rains3.com/img/docs/bandicam%202026-08-21%2000-54-38-346.mp4" type="video/mp4"></video>



## 注意

- 拖拽窗口时可能会出现窗口“抖动”的现象，这是正常情况。

## 相关

[底色](底色.md) · [悬底色](悬底色.md) · [按下](按下.md)
