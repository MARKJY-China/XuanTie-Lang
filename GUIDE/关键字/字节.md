 

# 字节

**原始字节流类型，用于底层内存、文件与网络 I/O。**

```text
设 数据: 字节
```

## 你可以……

- **在声明与外函数签名中作类型注解**：

```text
外 函 读原始(路径: 字): 字节
```

- **配合字符串做字节级访问**——`.字节(i)` 取第 `i` 个字节值（0–255 的 `整`），`.字节数` 取字节长度：

```text
示("Hi".字节(0))   // 72
示("你好".字节数)   // 6
```

## 注意

- `字节数` 按字节计，不是字符数——UTF-8 中一个汉字占 3 字节；按字符处理请用 `截取`（字符索引）。
- `.字节(i)` 返回的是 `整` 字节值，不是 `字节` 类型，别混淆。

## 相关

[字](字.md) · [数组](数组.md)
