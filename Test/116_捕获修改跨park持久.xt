// 108_捕获修改跨park持久(L2回归):fiber 内修改捕获变量,挂起唤醒后修改必须存活且不得堆损坏
// 历史Bug:捕获值经 alloca 借用,park 前修改仅落 alloca,唤醒后恢复为捕获时原值且引用计数错乱致堆损坏
示("--- 108_捕获修改跨park持久 开始 ---")

函 探() {
    设 x = "甲"
    设 计 = 10
    设 ch = 道(10)
    设 p = 异步 {
        时.睡(150)
        ch.发(1)
    }
    设 t = 异步 {
        x = "乙"
        计 = 计 + 5
        设 v = ch.收()
        // park 唤醒后:修改必须存活
        若 x == "乙" 且 计 == 15 且 v == 1 {
            示("PASS: 捕获修改跨park存活")
        } 否 {
            示("FAIL: x=#{x} 计=#{计} v=#{v}")
        }
    }
    等待(t)
    等待(p)
}
探()
示("--- 108_捕获修改跨park持久 结束 ---")
